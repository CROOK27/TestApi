# src/test/resources/application.yml
test:
  base-url: http://localhost:${server.port:8080}/api
  auth:
    username: "testuser"
    password: "testpassword"
    client-id: "test-client"
    client-secret: "test-secret"

# Можно оставить ту же базу или использовать H2
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_store_test  # Тестовая БД
    username: postgres
    password: 123456